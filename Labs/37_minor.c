#include <stdio.h>

int main(){

    // //1
    // int a = 0, k = 15;
    // int b = ++a > 0 ? (++k, k++) : (++a, a++);
    // printf("%d\n", a);
    // printf("%d\n", b);
    // printf("%d\n", k);

    // //2
    // int count = 1;
    // for(int i = 0; i < 4; i++){
    //     for(int j = 0; j < i + 1; j++){
    //         printf("%d ", count);
    //         count++;
    //     }
    //     printf("\n");
    // }

    // //3
    // int i, j = 0;
    // while(i = 5, j < 10){
    //     i++;
    //     j++;
    // }
    // printf("%d ", i);
    // printf("%d ", j);

    // //4
    // int a = 29;
    // int *ptr = &a;
    // printf("%d\n", (*ptr)++);
    // printf("%d\n", *ptr++);
    // printf("%d\n", ++ptr);

    // //5
    // int a = -1, b = -a;
    // int x, y;
    // x = (a > 0) && (b < 0) || (a < 0) && (b > 0);
    // y = (a <= 0) || (b >= 0) && (a >= 0) || (b <= 0);
    // printf("%d\n", x == y);

    // //6
    // int i;
    // for( i = 0; i < 5; i++){
    //     int i = 10;
    //     printf("%d ", i);
    //     i++;
    // }

    // //7
    // unsigned char a = 0xFA;
    // // printf("%d", a << 4);
    // a = (a >> 4) | (a << 4);
    // printf("%d\n", a); //175

    // //8
    // char a = 'P';
    // char b = 'x';
    // char c = a & b;
    // char e = a ^ b;
    // printf("%d %d", c, e);
}

